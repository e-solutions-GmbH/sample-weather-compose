<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="de.eso.weather.WeatherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:ignore="GoogleAppIndexingWarning"
        android:fullBackupContent="true">
        <activity
            android:name=".ui.WeatherActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.location.search.LocationSearchActivity"
            android:theme="@style/SearchLocationTheme"
            android:exported="false"
            android:label="@string/location_add_title"
            android:launchMode="singleTask" />

        <provider
            android:name="de.eso.weather.InitProvider"
            android:authorities="de.eso.weather.compose"
            android:exported="false"
            android:initOrder="1000" />
    </application>
</manifest>
